# Production environment - Vercel deployment
# Use /backend path to avoid conflict with Next.js mock API routes
# Vercel rewrites will proxy /backend/* to the real backend server
NEXT_PUBLIC_API_URL=/backend
